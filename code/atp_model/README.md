# Main model
This directory contains the code for the main prediction model of our project.

## Data

Currently 7 models have been defined. The models used in the presentation are Formula 0(Basic model), Formula 1(Basic model + win-streak), and Formula 3(Rel. ranking model (baseline)).
